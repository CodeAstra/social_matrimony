<% if @save_success %>
  $('#partner-pref-trigger').tab('show')
<% else %>
  // alert("Unable to save the data, Please Try with valid data");
  alert('<%= j @candidate.errors.full_messages.join(", ") %>');
<% end %>